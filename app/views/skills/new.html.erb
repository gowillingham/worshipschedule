<div class="content shadow">
  <div class="header">
    <h2>New skill for <%= @team.name %></h2>
  </div>
  <div class="form">
    <%= form_for @skill, :url => { :action => 'create' } do |f| %>
      <%= render 'shared/errors', :object => @skill %>
      <%= render 'fields', :f => f %>      
      <div class="footer">
        <%= f.submit "Save changes" %>
        or <%= link_to "Cancel", @team, :class => 'cancel' %>
      </div>
    <% end %>
  </div>
</div>